const HOME = "/";

const LOGIN = "/login";
const LOGOUT = "/logout";

const TODAY_AUTH = "/authentication";

const AUTH = "/auth";
const AUTH_REQUEST = "/request";
// const AUTH_REQUEST_LIST = "/list";

const KAKAO_AUTH = "/kakao";
const KAKAO_CALLBACK = "/login/kakao/callback";

export const routes = {
  home: HOME,
  login: LOGIN,
  logout: LOGOUT,
  today_auth: TODAY_AUTH,
  auth: AUTH,
  auth_request: AUTH_REQUEST,
  kakaoAuth: KAKAO_AUTH,
  kakaoCallback: KAKAO_CALLBACK
};
