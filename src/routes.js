const HOME = "/";

const LOGIN = "/login";
const LOGOUT = "/logout";

const TODAY_AUTH = "/authentication";

export const routes = {
  home:HOME,
  login:LOGIN,
  logout:LOGOUT,
  today_auth:TODAY_AUTH
};
